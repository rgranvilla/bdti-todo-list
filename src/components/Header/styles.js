import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  height: 5rem;

  background: var(--primary);
  color: var(--txt-white);

  display: flex;
  align-items: center;

  h1 {
    padding-left: 2rem;
  }
`;
